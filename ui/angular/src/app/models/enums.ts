export enum AccountTypeEnum {
    DEBIT = 'DEBIT',
    SAVING = 'SAVING'
}

export enum CurrencyEnum {
    GLD ='GLD'
}

export enum TransactionTypeEnum {
    TRANSFER = 'TRANSFER',
    DEPOSIT = 'DEPOSIT'
}